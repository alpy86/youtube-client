import { SortByDateDirective } from './sort-by-date.directive';
import { stringify } from '@angular/compiler/src/util';

describe('SortByDateDirective', () => {
  it('should create an instance', () => {
    // const directive: SortByDateDirective = new SortByDateDirective();
    // expect(directive).toBeTruthy();
  });
});
